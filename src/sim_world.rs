use crate::chunk_store::ChunkStore;

pub struct SimWorld;

impl SimWorld {
    pub fn step_region(&mut self, _store: &mut ChunkStore) {
        todo!("Simulation region stepping is not implemented yet")
    }
}
